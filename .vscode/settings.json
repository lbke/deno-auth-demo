{
    "deno.enable": true,
    "deno.unstable": true,
    // TODO: we should not need this, it breaks webapp
    // see readme for more info
    "deno.importMap": "./api/import_map.json",
    "deno.enablePaths": [
        "./api",
        "./webapp"
    ]
}